# README examples

This directory is reserved for the two organizer-approved qualitative examples
embedded in the project README:

- `method6b_session004.gif` and `method6b_session004.png`
- `method6b_session007.gif` and `method6b_session007.png`

Regenerate them with the commands in the README. The GIFs are compact previews;
the PNG files are publication-quality final-frame grids. Raw dataset frames are
not stored here.
